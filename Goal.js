import React, { Component } from 'react';
import { ListItem, Button } from 'react-native-elements';
import { Text } from 'react-native';


export default Goal = function({ goal }){
	
	return(
		<ListItem title={goal} />
			
	)
}